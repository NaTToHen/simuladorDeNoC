fun main(args: Array<String>) {
    val quantNodos = readln()
    val source = readln()
    val target = readln()

    if(source.toInt() < quantNodos.toInt() || target.toInt() < quantNodos.toInt()) {
            println("esse nodo não existe")
    } else {
        println()
        (1..quantNodos.toInt()).forEach { i ->

        }
    }


}